let resultadoActual = null;

function abrirMenu(){document.getElementById('sidebar').classList.add('open');document.getElementById('overlay').classList.add('show');document.body.style.overflow='hidden'}
function cerrarMenu(){document.getElementById('sidebar').classList.remove('open');document.getElementById('overlay').classList.remove('show');document.body.style.overflow=''}
function toggleSubmenu(button){const grupo=button.closest('.menu-group');const abierto=grupo.classList.contains('open');document.querySelectorAll('.menu-group.open').forEach(g=>{g.classList.remove('open');const b=g.querySelector('.menu-parent');if(b)b.setAttribute('aria-expanded','false')});if(!abierto){grupo.classList.add('open');button.setAttribute('aria-expanded','true')}}
async function cargarUsuario(){try{const r=await fetch('/api/session');if(!r.ok){location.href='/html/login.html';return}const d=await r.json();document.getElementById('profile-name').textContent=d.usuario||'-';document.getElementById('profile-user').textContent=d.nombre||d.usuario||'-'}catch(e){location.href='/html/login.html'}}
function dinero(v){const n=Number(v||0);return Number.isFinite(n)?n.toLocaleString('es-EC',{minimumFractionDigits:2,maximumFractionDigits:2}):'0.00'}
function escapar(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function inicializarFiltros(){const mes=document.getElementById('mes'),anio=document.getElementById('anio');for(let i=1;i<=12;i++){const o=document.createElement('option');o.value=String(i).padStart(2,'0');o.textContent=String(i).padStart(2,'0');mes.appendChild(o)}const actual=new Date().getFullYear();for(let i=0;i<5;i++){const o=document.createElement('option');o.value=actual-i;o.textContent=actual-i;anio.appendChild(o)}mes.value=String(new Date().getMonth()+1).padStart(2,'0');anio.value=actual;['nivel','mes','anio'].forEach(id=>document.getElementById(id).addEventListener('change',cargarBalance))}
function pintarLista(id,rows){const el=document.getElementById(id);let html='';for(const r of rows||[]){const nivel=Math.max(0,Number(r.nivel)||0);const raiz=String(r.codCuenta||'').length===1;html+=`<div class="account-row ${raiz?'root-account':''}" style="--indent:${nivel}"><span class="account-name">${escapar(r.nomCuenta)}</span><span class="account-value">${dinero(r.salFinPer)}</span><span class="account-period">${dinero(r.valorPeriodo)}</span></div>`}el.innerHTML=html||'<div class="no-data">Sin cuentas con saldo.</div>'}
function pintarBalance(d){resultadoActual=d;document.getElementById('periodo').textContent=`PERIODO FISCAL: ${d.mes}/${d.anio}     ${d.nivel}`;pintarLista('ingresos',d.ingresos);pintarLista('gastos',d.gastos);document.getElementById('total-ingresos').textContent=dinero(d.totalIngresos);document.getElementById('total-ingresos-periodo').textContent=dinero(d.totalIngresosPeriodo);document.getElementById('total-gastos').textContent=dinero(d.totalGastos);document.getElementById('total-gastos-periodo').textContent=dinero(d.totalGastosPeriodo);document.getElementById('res-ing-acum').textContent=dinero(d.totalIngresos);document.getElementById('res-ing-per').textContent=dinero(d.totalIngresosPeriodo);document.getElementById('res-gas-acum').textContent=dinero(d.totalGastos);document.getElementById('res-gas-per').textContent=dinero(d.totalGastosPeriodo);document.getElementById('resultado-ejercicio').textContent=dinero(d.resultadoEjercicio);document.getElementById('resultado-periodo').textContent=dinero(d.resultadoPeriodo);document.getElementById('resultado-ejercicio').classList.toggle('negativo',Number(d.resultadoEjercicio)<0);document.getElementById('resultado-periodo').classList.toggle('negativo',Number(d.resultadoPeriodo)<0);document.getElementById('btn-print').disabled=false;document.getElementById('btn-pdf').disabled=false}
async function cargarBalance(){const params=new URLSearchParams({mes:document.getElementById('mes').value,anio:document.getElementById('anio').value,nivel:document.getElementById('nivel').value});document.getElementById('btn-print').disabled=true;document.getElementById('btn-pdf').disabled=true;['ingresos','gastos'].forEach(id=>document.getElementById(id).innerHTML='<div class="loading">Consultando...</div>');try{const r=await fetch(`/api/balresul?${params.toString()}&_=${Date.now()}`);const text=await r.text();let d={};try{d=JSON.parse(text)}catch(_){throw new Error(`HTTP ${r.status}: respuesta no válida`)}if(!r.ok)throw new Error(d.detail?`${d.error||'Error'} ${d.detail}`:(d.error||`HTTP ${r.status}`));pintarBalance(d)}catch(e){resultadoActual=null;const msg=`<div class="error">${escapar(e.message)}</div>`;['ingresos','gastos'].forEach(id=>document.getElementById(id).innerHTML=msg)}}
function filasPDF(){const filas=[];const agregar=(titulo,rows,total)=>{filas.push({tipo:'seccion',texto:titulo});for(const r of rows||[])filas.push({tipo:'cuenta',texto:r.nomCuenta||'',valor:dinero(r.salFinPer),periodo:dinero(r.valorPeriodo),nivel:Number(r.nivel)||0,raiz:String(r.codCuenta||'').length===1});filas.push({tipo:'total',texto:`TOTAL ${titulo}:`,valor:dinero(total)})};agregar('INGRESOS',resultadoActual.ingresos,resultadoActual.totalIngresos);agregar('COSTOS Y GASTOS',resultadoActual.gastos,resultadoActual.totalGastos);return filas}
function encabezadoPDF(doc,pagina){doc.setFont('helvetica','normal');doc.setFontSize(8);doc.text('Complete Accounting System - CONTABILIDAD',5,8);doc.setFont('helvetica','bold');doc.setFontSize(18);doc.text('BALANCE DE RESULTADOS',105,17,{align:'center'});doc.setFontSize(8);doc.text(`PERIODO FISCAL: ${resultadoActual.mes}/${resultadoActual.anio}     ${resultadoActual.nivel}`,5,25);doc.setLineWidth(.35);doc.line(5,29,205,29);doc.setFontSize(9);doc.text('CUENTAS',8,35);doc.text('VALORES',160,35);doc.line(5,37,205,37);doc.setFont('helvetica','normal');doc.setFontSize(7);doc.text(`pag. ${pagina}`,205,289,{align:'right'})}
function nuevaPaginaPDF(doc,pagina){doc.addPage();encabezadoPDF(doc,pagina);return 43}
function exportarPDF(){if(!resultadoActual)return;if(!window.jspdf?.jsPDF){alert('No se pudo cargar el generador de PDF.');return}const {jsPDF}=window.jspdf;const doc=new jsPDF({orientation:'portrait',unit:'mm',format:'a4'});let pagina=1,y=43;encabezadoPDF(doc,pagina);for(const f of filasPDF()){if(y>279){pagina++;y=nuevaPaginaPDF(doc,pagina)}if(f.tipo==='seccion'){doc.setFont('helvetica','bold');doc.setFontSize(8);doc.text(f.texto,8,y)}else if(f.tipo==='cuenta'){doc.setFont('helvetica',f.raiz?'bold':'normal');doc.setFontSize(8);const indent=Math.min(45,(f.nivel||0)*3);doc.text(f.texto,8+indent,y,{maxWidth:145});doc.text(f.valor,198,y,{align:'right'})}else{doc.setFont('helvetica','bold');doc.setFontSize(9);doc.text(f.texto,145,y,{align:'right'});doc.text(f.valor,198,y,{align:'right'})}y+=f.tipo==='cuenta'?4:f.tipo==='total'?6:5}if(y>264){pagina++;y=nuevaPaginaPDF(doc,pagina)}y+=5;doc.setFont('helvetica','bold');doc.setFontSize(9);doc.text(`TOTAL INGRESOS:   ACUMULADO: ${dinero(resultadoActual.totalIngresos)}     PERÍODO: ${dinero(resultadoActual.totalIngresosPeriodo)}`,105,y,{align:'center'});y+=6;doc.text(`TOTAL GASTOS:     ACUMULADO: ${dinero(resultadoActual.totalGastos)}     PERÍODO: ${dinero(resultadoActual.totalGastosPeriodo)}`,105,y,{align:'center'});doc.save(`BalanceResultados_${resultadoActual.mes}_${resultadoActual.anio}.pdf`)}
function imprimir(){window.print()}
async function cerrarSesion(){try{await fetch('/api/logout',{method:'POST'})}finally{location.href='/html/login.html'}}
document.addEventListener('DOMContentLoaded',()=>{cargarUsuario();inicializarFiltros();cargarBalance()});
